Stacey is not quite ready for production.

The general idea is to create an app which takes static content from .txt files, image files and directory structures and generates a static portfolio website. So, a no-cms, no-database, sort-of-dynamic website generator.

If you look in the /content and /app/templates folders, you should get the general idea of how it all works.
Point your browser at it to see it do what it does.
